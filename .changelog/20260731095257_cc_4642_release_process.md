---
type: Other
closes:
  - ckeditor/ckeditor5-internal#4642
---

The repository has a standardized release process now: changes are collected as changelog entries, the version is bumped in every file that stores it by a script, and every release gets a git tag and a GitHub release page.

See [the release process documentation](https://github.com/ckeditor/skills/blob/main/docs/release-process.md) for details.
